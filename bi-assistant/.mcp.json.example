{
  "mcpServers": {
    "databricks-sql": {
      "type": "http",
      "url": "${DATABRICKS_MCP_ENDPOINT}",
      "headers": {
        "Authorization": "Bearer ${DATABRICKS_TOKEN}"
      }
    }
  }
}
